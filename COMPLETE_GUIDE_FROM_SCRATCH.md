# 🚀 ПОЛНАЯ ИНСТРУКЦИЯ С НУЛЯ - Railway

## 📋 ЧТО МЫ СДЕЛАЕМ:

1. Подготовим проект локально
2. Создадим бота в Telegram
3. Загрузим на GitHub
4. Развернём на Railway
5. Настроим PostgreSQL
6. Запустим всё

**Время: 20-30 минут**

---

## 📂 ШАГ 1: ПОДГОТОВКА ПРОЕКТА

### **1.1. Создай папку проекта:**

```bash
mkdir nftg-channel-bot
cd nftg-channel-bot
```

### **1.2. Скачай ВСЕ файлы из outputs:**

Тебе нужны эти файлы:
- `server-FIXED.js` → переименуй в `server.js`
- `interactive-bot.js`
- `start.js`
- `package.json`
- `.env.example`

### **1.3. Структура должна быть:**

```
nftg-channel-bot/
├── server.js           ← из server-FIXED.js
├── interactive-bot.js
├── start.js
├── package.json
├── .env.example
├── .gitignore         ← создадим
└── README.md          ← опционально
```

### **1.4. Создай .gitignore:**

```bash
# В корне проекта создай файл .gitignore
cat > .gitignore << EOF
node_modules/
.env
*.log
.DS_Store
EOF
```

### **1.5. Создай .env для локального тестирования:**

```bash
cp .env.example .env
```

Отредактируй `.env`:
```env
# Пока пустые, заполним после создания бота
BOT_TOKEN=
BOT_API_KEY=your_random_key_123

# Локальные настройки
API_URL=http://localhost:3000/api
PORT=3000
UPDATE_INTERVAL=6

# Локальная БД (для тестирования)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nftg_zonix
DB_USER=postgres
DB_PASSWORD=твой_локальный_пароль
```

---

## 🤖 ШАГ 2: СОЗДАНИЕ TELEGRAM БОТА

### **2.1. Открой Telegram:**

1. Найди `@BotFather`
2. Отправь `/newbot`

### **2.2. Настрой бота:**

```
BotFather: Alright, a new bot. How are we going to call it?
Ты: NFTG Channel Stats Bot

BotFather: Good. Now let's choose a username for your bot.
Ты: nftg_channel_stats_bot
(или любой другой свободный)

BotFather: Done! 
Use this token to access the HTTP API:
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

### **2.3. СКОПИРУЙ ТОКЕН!**

Добавь в `.env`:
```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

### **2.4. Настрой команды (опционально):**

```
/setcommands в @BotFather

start - Начать работу
my_channels - Мои каналы
help - Помощь
```

---

## 💻 ШАГ 3: ЛОКАЛЬНОЕ ТЕСТИРОВАНИЕ (необязательно, но рекомендую)

### **3.1. Установи PostgreSQL локально:**

**На macOS:**
```bash
brew install postgresql
brew services start postgresql
createdb nftg_zonix
```

**На Ubuntu/Debian:**
```bash
sudo apt install postgresql
sudo systemctl start postgresql
sudo -u postgres createdb nftg_zonix
```

**На Windows:**
- Скачай с postgresql.org
- Установи
- Создай БД через pgAdmin

### **3.2. Установи зависимости:**

```bash
npm install
```

### **3.3. Запусти локально:**

```bash
# Терминал 1
node server.js

# Должно быть:
# ✅ Подключено к PostgreSQL
# ✅ Таблицы созданы
# 🚀 Server running on port 3000

# Терминал 2
node interactive-bot.js

# Должно быть:
# 🤖 Бот запущен!
# API URL: http://localhost:3000/api
```

### **3.4. Протестируй бота:**

1. Найди своего бота в Telegram
2. Отправь `/start`
3. Должен ответить приветствием

**Если работает локально - отлично! Переходим к деплою.**

**Если НЕ работает - останови здесь, разберёмся.**

---

## 📤 ШАГ 4: ЗАГРУЗКА НА GITHUB

### **4.1. Инициализируй Git:**

```bash
cd nftg-channel-bot
git init
git add .
git commit -m "Initial commit: NFTG Channel Bot"
```

### **4.2. Создай репозиторий на GitHub:**

1. Зайди на github.com
2. New Repository
3. Название: `nftg-channel-bot`
4. Public или Private (неважно)
5. НЕ добавляй README, .gitignore (у нас уже есть)
6. Create Repository

### **4.3. Залей код:**

```bash
# GitHub покажет команды, используй их:
git remote add origin https://github.com/твой-username/nftg-channel-bot.git
git branch -M main
git push -u origin main
```

### **4.4. Проверь:**

Обнови страницу на GitHub - должны быть все файлы:
- ✅ server.js
- ✅ interactive-bot.js
- ✅ start.js
- ✅ package.json
- ✅ .gitignore
- ❌ .env (не должен быть! это секрет)

---

## 🚂 ШАГ 5: ДЕПЛОЙ НА RAILWAY

### **5.1. Регистрация:**

1. Зайди на `railway.app`
2. "Login" → "Login with GitHub"
3. Authorize Railway
4. Подтверди email если попросит

### **5.2. Создай проект:**

1. Dashboard → "New Project"
2. "Deploy from GitHub repo"
3. Выбери репозиторий `nftg-channel-bot`
4. Railway начнёт деплой автоматически

**ПОДОЖДИ:** Первый деплой может упасть - это нормально! Нужно настроить БД.

### **5.3. Добавь PostgreSQL:**

```
1. В проекте кликни "+ New"
2. "Database" → "Add PostgreSQL"
3. Railway создаст БД

Теперь должно быть:
├── nftg-channel-bot (твой сервис)
└── Postgres (БД)
```

### **5.4. Подключи PostgreSQL к сервису:**

```
1. Кликни на "Postgres"
2. Справа кнопка "Connect"
3. Выбери "nftg-channel-bot"
4. ✅ Подтверди

Railway автоматически добавит переменные!
```

### **5.5. Настрой переменные окружения:**

```
Railway → nftg-channel-bot → Variables → RAW Editor

Вставь (замени на свои значения):

BOT_TOKEN=твой_токен_от_BotFather
BOT_API_KEY=твой_случайный_ключ_abc123
API_URL=http://localhost:3000/api
PORT=3000
UPDATE_INTERVAL=6
```

**ВАЖНО:** 
- НЕ добавляй DATABASE переменные вручную
- Railway добавила их автоматически при подключении Postgres

### **5.6. Настрой Start Command:**

```
Railway → nftg-channel-bot → Settings

Deploy → Custom Start Command:
node start.js

Сохрани
```

### **5.7. Триггер редеплоя:**

```
Railway → nftg-channel-bot → Deployments

Кликни "..." → "Redeploy"
```

---

## 📊 ШАГ 6: ПРОВЕРКА

### **6.1. Смотри логи:**

```
Railway → Deployments → Latest → View Logs

Должно быть:

🚀 Запуск NFTG Channel Bot...
📡 Запуск API сервера...
✅ API сервер запущен

🔌 Пытаюсь подключиться к PostgreSQL...
   Host: containers-us-west-xxx.railway.app
   Database: railway
   User: postgres
✅ Подключено к PostgreSQL
   Database: railway
✅ Таблицы созданы/проверены
✅ Триггер для рейтингов создан
✅ Категории загружены
🚀 Server running on port 3000

🤖 Запуск Telegram бота...
✅ Бот запущен
🤖 Интерактивный бот запущен!
   Username: @твой_бот
   API URL: http://localhost:3000/api
✅ Автообновление настроено
```

### **6.2. Проверь переменные:**

```
Railway → nftg-channel-bot → Variables

Должно быть:

Твои переменные:
BOT_TOKEN=123...
BOT_API_KEY=abc...
API_URL=http://localhost:3000/api
PORT=3000
UPDATE_INTERVAL=6

Railway добавила автоматически:
PGDATABASE=railway
PGHOST=containers-us-west-xxx.railway.app
PGPASSWORD=длинный_пароль
PGPORT=5432
PGUSER=postgres
POSTGRES_PASSWORD=такой_же_пароль
RAILWAY_PRIVATE_DOMAIN=...
```

**Проверь:** Нет ли `${{переменная}}`? Должны быть реальные значения!

### **6.3. Тест API в браузере:**

```
Railway → nftg-channel-bot → Settings → Networking

Generate Domain (если нет)

Скопируй URL (например):
https://nftg-channel-bot-production.up.railway.app

Открой в браузере:
https://nftg-channel-bot-production.up.railway.app/

Должно показать JSON:
{
  "service": "NFTG-ZONIX Channel Statistics API",
  "version": "2.0.0",
  ...
}
```

### **6.4. Тест бота в Telegram:**

```
1. Найди своего бота в Telegram
2. /start
3. Должен ответить с кнопками
4. Нажми "📢 Мои каналы"
5. Должен сказать что каналов нет
```

---

## ✅ ШАГ 7: ДОБАВЬ СВОЙ КАНАЛ

### **7.1. Добавь бота в админы:**

```
1. Открой свой тестовый канал в Telegram
2. Channel Settings → Administrators
3. Add Administrator
4. Найди своего бота
5. Дай права:
   ✅ View channel stats (ОБЯЗАТЕЛЬНО!)
   ⚪ Остальные по желанию
6. Save
```

### **7.2. Проверь что бот заметил:**

```
Логи Railway должны показать:
✅ Бот добавлен как админ: Твой Канал (ID)
```

Также бот отправит тебе сообщение в личку!

### **7.3. Настрой канал:**

```
1. Открой бота
2. /my_channels
3. Кликни на свой канал
4. Выбери до 3 категорий (кликай кнопки)
5. Подтверди
6. Напиши описание канала
7. Посмотри Preview
8. Опубликовать!
```

### **7.4. Проверь в БД:**

```
Railway → Postgres → Data (вкладка)

Выбери таблицу "channels"

Должна быть запись с твоим каналом!
```

---

## 🎉 ГОТОВО!

Если всё прошло успешно:

- ✅ Бот работает
- ✅ API работает
- ✅ PostgreSQL подключена
- ✅ Канал добавлен и настроен
- ✅ Данные в БД

---

## 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ:

### **Проблема 1: Deployment failed**

**Смотри логи:**
```
Railway → Deployments → Failed → View Logs
Скопируй и покажи мне последние 30 строк
```

### **Проблема 2: ECONNREFUSED**

**Проверь:**
```
1. Postgres подключена к сервису?
   Postgres → Connected Services → должен быть nftg-channel-bot

2. API_URL правильный?
   Variables → API_URL → должен быть http://localhost:3000/api

3. Start Command правильный?
   Settings → должен быть node start.js
```

### **Проблема 3: Бот не отвечает**

**Проверь:**
```
1. Логи показывают "Бот запущен"?
2. BOT_TOKEN правильный?
3. Бот запущен на Railway (не только локально)?
```

---

## 📸 ЧЕКЛИСТ:

Отметь что сделал:

```
✅ Создал папку проекта
✅ Скачал все файлы из outputs
✅ Создал бота в @BotFather
✅ Получил BOT_TOKEN
✅ Протестировал локально (опционально)
✅ Залил на GitHub
✅ Создал проект на Railway
✅ Добавил PostgreSQL
✅ Подключил Postgres к сервису
✅ Настроил переменные
✅ Start Command: node start.js
✅ Редеплой
✅ Логи показывают успех
✅ API отвечает в браузере
✅ Бот отвечает в Telegram
✅ Добавил бота в канал как админа
✅ Настроил канал через бота
✅ Канал появился в БД
```

---

## 🎯 НАЧИНАЙ С ШАГА 1!

**Делай по порядку, не пропускай шаги.**

**Если застрял - останови и скажи на каком шаге проблема!** 

Готов начать? 🚀
